# AGENTS.md

This repository keeps the durable agent/developer contract in
[`CLAUDE.md`](./CLAUDE.md). Codex and other agent tools should use that file for
project architecture, commands, and current conventions.

Keep this file as a short pointer rather than duplicating the full instructions;
the goal is to avoid drift between agent entry points.
